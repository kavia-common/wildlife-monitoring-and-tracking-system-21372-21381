# MongoDB connection configuration for wildlife tracking backend

# PUBLIC: MongoDB connection string (SRV or standard). Example:
# mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017

# PUBLIC: Database name to use
MONGODB_DB_NAME=wildlife_tracking
